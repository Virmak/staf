test suite "dddd"
IMPORTS
    Import selenium
    Import "./relative_file"
VARS
    $test = [1, 2, 3]
    $x = 'okson'
    $var = kw call()
KEYWORDS
    Do login ($test, $tete)
        Ok boy('test', 123, ['a', 1], $ok, $test, {ok: 1})
        FOR $item IN $list
        END FOR
    RETURN {'DICT': Okthankyou($test)}

TEST CASES
    ttt login page:
        Do login($te)
        FOR $i in $list
            test keyword
        END FOR
        okkk
    END
    TC2 OK:
        do nothing
    END